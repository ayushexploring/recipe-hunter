<% unless params[:search].nil? %>
	<div>
		<% if !params[:search].empty? %>
			<h4>Results for <%= params[:search] %> (<%= @recipes.count %>)</h4>
		<% else %>
			<h4>Check out some of our top Recipes</h4>
		<% end %>
		<hr>
		<!-- <table class="table table-striped">
			<thead class="black white-text" class="black">
			  <tr>
			  	<th scope="col">#</th>
			  	<th scope="col">Recipe</th>
			  	<th scope="col">Title</th>
			  	<th scope="col">Publisher</th>
			  	<th scope="col">Social Rank</th>
			  </tr>
			</thead>
			<tbody>
			  <% x = 0 %>
			  <% @recipes.each do |recipe| %>
			    <tr>
			    	<th scope="row"><%= x+= 1 %></th>
			      	<td id="image"><%= link_to(image_tag(recipe["image_url"]), recipe['f2f_url'])%></td>
			      	<td><%= link_to(sanitize(recipe["title"]), recipe['f2f_url']) %></td>
			      	<td><%= link_to(sanitize(recipe["publisher"]), recipe['publisher_url']) %></td>
			      	<td><%= recipe["social_rank"] %></td>
			    </tr>
			  <% end %>
			</tbody>
		</table> -->
		<div class="parent">
			<% @recipes.each do |recipe| %>
				<div class="card">
		  			<%= link_to(image_tag(recipe["image_url"]), recipe['f2f_url'], target: :_blank)%>
		  			<div class="container"><br>
		    			<p><b>Title: <%= link_to(sanitize(recipe["title"]), recipe['f2f_url'], target: :_blank) %></b></p>
		    			<p><b>Publisher:
		    			<%= link_to(sanitize(recipe["publisher"]), recipe['publisher_url'], target: :_blank) %>
		    			</b></p>
		    			<p><b>Social Rank: <%= recipe["social_rank"] %></b></p>
		  			</div>
				</div>
			<% end %>
		</div>
	</div>
<% end %>
